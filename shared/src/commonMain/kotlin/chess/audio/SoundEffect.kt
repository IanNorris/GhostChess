package chess.audio

/**
 * Sound effects that can be triggered during gameplay.
 */
enum class SoundEffect {
    MOVE,
    CAPTURE,
    CHECK,
    CHECKMATE,
    CASTLE,
    ILLEGAL,
    UNDO,
    GAME_START,
    DRAW
}
